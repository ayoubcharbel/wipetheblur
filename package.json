{
  "name": "telegram-bot-wipe-the-blur",
  "version": "1.0.0",
  "description": "Telegram Activity Tracker Bot for WipeTheBlur.com",
  "main": "api/telegram-webhook.js",
  "scripts": {
    "start": "node bot.js",
    "dev": "nodemon bot.js",
    "build": "mkdir -p public && cp *.html *.css *.js public/ && cp -r images public/ 2>/dev/null || true"
  },
  "keywords": [
    "telegram",
    "bot",
    "activity-tracker",
    "leaderboard"
  ],
  "dependencies": {
    "express": "^4.19.2",
    "node-telegram-bot-api": "^0.66.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  },
  "engines": {
    "node": ">=18"
  }
}